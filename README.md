# db-dash-demo-db

demo weaviate db for [db dash](https://github.com/JanikThePanic/db-dash)

download the compose file and run `docker-compose up -d`

boom you got a weaviate db (exposed at port 3131) with some basic demo data to test db dash with (or anything else you like)
